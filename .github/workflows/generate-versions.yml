name: Generate versions index

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    env:
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Generate versions.json
        run: python scripts/generate_versions.py

      - name: Commit versions.json if changed
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'maj versions.json'
          file_pattern: versions.json index.html

      - name: Notify Discord on success
        if: ${{ success() && env.DISCORD_WEBHOOK_URL != '' }}
        env:
          PAGE_URL: https://neotok.github.io/updatePz/
        run: |
          latest_version=$(python -c "import json; data=json.load(open('versions.json', encoding='utf-8')); print(data[0]['name'] if data else 'N/A')")

          payload=$(cat <<EOF
          {
            "content": "Traduction des mises a jours de Project Zomboid\nVersion : ${latest_version}\n${PAGE_URL}"
          }
          EOF
          )

          curl \
            --fail \
            --show-error \
            --silent \
            -H "Content-Type: application/json" \
            -X POST \
            --data "$payload" \
            "$DISCORD_WEBHOOK_URL"